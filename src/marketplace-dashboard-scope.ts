/** Re-exported from data.ts (keeps page imports stable; avoids circular imports in data layer). */
export {
  isMarketplaceDashboardSheetCategory,
  productMatchesMarketplaceDashboardScope,
} from "./data";
